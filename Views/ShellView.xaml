<Window x:Class="Khsw.Instrument.Demo.Views.ShellView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Khsw.Instrument.Demo"
        xmlns:prism="http://prismlibrary.com/"
        xmlns:region="clr-namespace:Khsw.Instrument.Demo.Infrastructures"
        xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
        Style="{StaticResource MaterialDesignWindow}"
        mc:Ignorable="d"
        prism:RegionManager.RegionManager="{Binding RegionMannager}"
        prism:ViewModelLocator.AutoWireViewModel="True"
        Title="{Binding Title}" Height="740" Width="1200">

    <Grid>
        <TabControl>
            <TabItem Header="设备1">
                <ContentControl prism:RegionManager.RegionName="{x:Static region:RegionNames.ControlDemoContentRegion}" Margin="5"/>
            </TabItem>

            <!--<TabItem Header="设备2">
                <ContentControl prism:RegionManager.RegionName="{x:Static region:RegionNames.OtherDemoContentRegion}" Margin="5"/>
            </TabItem>-->
        </TabControl>
    </Grid>
</Window>
